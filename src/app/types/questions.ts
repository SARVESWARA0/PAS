export interface InnovationQuestion {
    id: string
    question: string
  }
  
  export interface EmailScenario {
    id: string
    subject: string
    context: string
    instructions: string
  }
  
  